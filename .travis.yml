language: java

sudo: false

script:
  - npm test && npm run build
  - mvn validate clean install | egrep -v "(^\[INFO\]|^\[DEBUG\])"
cache:
  directories:
  - "$HOME/.m2"

jdk:
  - oraclejdk8

before-install:
  - cd webapp/

install:
  - npm install 	
