language: java

sudo: false

script:
  mvn validate clean install -q | egrep -v "(^\[INFO\])"
cache:
  directories:
  - "$HOME/.m2"

jdk:
  - oraclejdk8

before-install:
  - cd webapp/

install:
  - npm install 	
