language: java

sudo: false

before_script:
 - cd src/main/webapp/
 - npm test && npm run build

script:
  mvn validate clean install | egrep -v "(^\[INFO\]|^\[DEBUG\]^\INFO\)"
cache:
  directories:
  - "$HOME/.m2"

jdk:
  - oraclejdk8

before-install
 - cd src/main/webapp/

install:
  - npm install 	
